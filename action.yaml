name: Initialize Cloudify Profile
author: Isaac Shabtay
description: Initializes a Cloudify CLI profile
branding:
  icon: cloud
  color: white
runs:
  using: docker
  image: docker://cloudifyplatform/cloudify-cli:5.1.1
  entrypoint: cfyci
  args:
    - init
